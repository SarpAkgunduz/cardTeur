---
description: "NPM dependency registry, required installations, and troubleshooting guide for CardTeur."
applyTo: "**"
---

# CardTeur Dependency Registry

Consolidated into `CLAUDE.md` at the repo root (## Key dependencies, ## Environment variables, ## Known gotchas sections) — read that instead, it's the maintained source. This file previously included a PowerShell-based troubleshooting checklist that assumed a Windows environment, which doesn't match this project's macOS setup — dropped rather than carried forward.
